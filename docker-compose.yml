services:
  geo-alert-frontend:
    build: .
    ports:
      - "5173:5173"
    volumes:
      - .:/app
      - /app/node_modules # 로컬의 node_modules가 컨테이너를 덮어쓰지 않게 보호
    env_file:
      - .env
    environment:
      - CHOKIDAR_USEPOLLING=true # 윈도우/맥 환경에서 파일 변경 감지를 확실하게 함
